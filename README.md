# ssh stunnel
```
curl -sSL https://raw.githubusercontent.com/noelrubio143/stunneling/refs/heads/main/stunnel-menu.sh -o /tmp/stunnel-menu.sh && sudo mv /tmp/stunnel-menu.sh /usr/local/bin/amber && sudo chmod +x /usr/local/bin/amber && echo "Installation complete. Type 'amber' to open the SSH + Stunnel menu."
```
